class Book{
    String title;
    String isbn;
    String author;
    String description;
    String cover;
    int copies;

    Book({
        required this.title,
        required this.isbn,
        required this.author,
        required this.description,
        required this.cover,
        required this.copies
    });
}